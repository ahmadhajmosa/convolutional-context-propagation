[project]
name = "ccp"
version = "0.1.0"
description = "Convolutional Context Propagation for long-context QA."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "dspy-ai==3.1.3",
  "openai>=2.0.0",
]

[tool.setuptools.packages.find]
include = ["ccp*"]
